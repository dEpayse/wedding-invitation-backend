spring.application.name=wedding-invitation

# Active Profile
# Use 'local' for local development (requires application-local.properties with password)
# Use 'prod' for production (requires environment variables)
spring.profiles.active=local

# Supabase PostgreSQL Database Configuration
# Using direct connection (Spring Boot's HikariCP handles connection pooling)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.npgqwlvrvgwsjxwlcrvf
# Password is set in application-local.properties (not committed to git)

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
